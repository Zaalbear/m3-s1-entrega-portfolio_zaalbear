import { ProjectsList } from "./ProjectsList"

export const ProjectsSection = () => {
  return (
    <section id="projectsSection">
        <ul>
            <ProjectsList/>
        </ul>
    </section>
  )
}